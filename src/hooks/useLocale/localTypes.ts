export type locale = {
  app: {
    simulationControls: {
      simulateAllBtnText: string;
      pauseBtnText: string;
      resumeBtnText: string;
      stopBtnText: string;
    }
  }
  markerSimulationMap: {
    uploadBtnLabel: string;
    fileUploadHint: string;
  }
}